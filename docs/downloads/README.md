# 下载与构建

仓库以 `docs/` 中的分章节 Markdown 为单一事实来源，不把重复的 Word、PDF 和大型快照直接提交到 Git 历史中。

- 运行 `python scripts/build_bundle.py` 可生成单文件 Markdown。
- 正式发布的 Word、PDF 与打包文件放在 GitHub Releases 中。
- 旧版本的变更说明保存在 `releases/`。
